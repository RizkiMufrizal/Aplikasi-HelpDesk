<!-- <%- model_class = Problem -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:title) %></th>
      <th><%= model_class.human_attribute_name(:description) %></th>
      <th><%= model_class.human_attribute_name(:picture) %></th>
      <th><%= model_class.human_attribute_name(:status) %></th>
      <th><%= model_class.human_attribute_name(:user_id) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @problems.each do |problem| %>
      <tr>
        <td><%= link_to problem.id, problem_path(problem) %></td>
        <td><%= problem.title %></td>
        <td><%= problem.description %></td>
        <td><%= problem.picture %></td>
        <td><%= problem.status %></td>
        <td><%= problem.user_id %></td>
        <td><%=l problem.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_problem_path(problem), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      problem_path(problem),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_problem_path,
            :class => 'btn btn-primary' %> -->

<div class="jumbotron text-center">
  <h1 class="display-3">Aplikasi Helpdesk</h1>
  <p class="lead">
    Hallo <%= session[:user_name] %>
  </p>
  <hr class="m-y-md">
  <p>Anda sudah melaporkan <a><%= Problem.where(user_id: session[:user_id]).count %></a> Masalah</p>
  <p><a><%= Problem.where(user_id: session[:user_id], status: 'selesai').count %></a> Masalah telah terselsaikan</p>
  <p><a><%= Problem.where(user_id: session[:user_id], status: 'sedang dikerjakan').count %></a> Masalah masih dalam penanganan</p>
</div>
